# Quality profiles for data validation
# TODO: Implement actual quality checks when building quality module

profiles:
  default_daily:
    continuity:
      enabled: true
      # TODO: Add configuration for continuity checks
      # max_gap_days: 7
    outliers:
      enabled: true
      # TODO: Add configuration for outlier detection
      # method: "iqr"  # interquartile range
      # threshold: 3.0  # z-score or IQR multiplier
    range:
      enabled: false
      # TODO: Add min/max value constraints
      # min_value: null
      # max_value: null

  default_monthly:
    continuity:
      enabled: true
      # TODO: Add configuration for continuity checks
      # max_gap_months: 3
    outliers:
      enabled: true
      # TODO: Add configuration for outlier detection
      # method: "iqr"
      # threshold: 3.0
    range:
      enabled: false
      # TODO: Add min/max value constraints

